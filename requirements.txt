fastapi==0.115.5
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0
PyJWT[crypto]>=2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.2.0
pydantic-settings==2.6.1
httpx==0.28.1
slowapi>=0.1.9
jinja2==3.1.6
apscheduler>=3.10,<4
Pillow>=10.0,<12
python-multipart>=0.0.9

# Observability (ADR-012) — traces + /metrics exported to a local Grafana
# Alloy collector, which forwards to Grafana Cloud. Inert unless OTEL_ENABLED=true.
# Capped at <8: prometheus-fastapi-instrumentator 8.x requires starlette>=1.0,
# which conflicts with fastapi==0.115.5 (needs starlette<0.42). 7.1.0 uses the
# same Instrumentator().instrument(app).expose(...) API used in app/core/telemetry.py.
prometheus-fastapi-instrumentator>=7.0,<8
opentelemetry-api>=1.29,<2
opentelemetry-sdk>=1.29,<2
opentelemetry-exporter-otlp-proto-http>=1.29,<2
opentelemetry-instrumentation-fastapi>=0.50b0
opentelemetry-instrumentation-httpx>=0.50b0
opentelemetry-instrumentation-sqlalchemy>=0.50b0
opentelemetry-instrumentation-logging>=0.50b0
